####
# Maintainer: Kamil Cukrowski <kamilcukrowski@gmail.com>

[ -e PKGBUILD ] && . PKGBUILD

package() {
	echo "PKGBUILD.testing build"
	cp -a ../../resources/* "$pkgdir"
	chown -R root:root "$pkgdir"
}
