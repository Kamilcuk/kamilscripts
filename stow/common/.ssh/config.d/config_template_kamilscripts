# .ssh/config/template
# Copy this to ~/.ssh/config to connect with github at least
# then install

Host gitlab.com github.com
	IdentityFile ~/.ssh/github_id_rsa

Host *
	Include ~/.ssh/config.d/*.conf

